name: CD Deploy

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Deploy to VM
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: course.prafdin.ru
        username: vboxuser
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: 2434
        script: |
          # Конфигурация для вашего FastAPI приложения
          REPO_URL="https://github.com/micra07/devops.git"
          DEPLOY_DIR="/home/vboxuser/catty-app"
          BRANCH="actions"
          SERVICE_NAME="catty-app"  # имя вашего systemd сервиса

          echo "=== Starting deployment for FastAPI app ==="

          # 1. Обновляем или клонируем репозиторий
          if [ -d "$DEPLOY_DIR" ]; then
            echo "Updating existing repository..."
            cd "$DEPLOY_DIR"
            git fetch origin
            git checkout $BRANCH
            git pull origin $BRANCH
          else
            echo "Cloning repository..."
            mkdir -p "$DEPLOY_DIR"
            git clone -b $BRANCH "$REPO_URL" "$DEPLOY_DIR"
            cd "$DEPLOY_DIR"
          fi

          # 2. Активируем виртуальное окружение и устанавливаем зависимости
          echo "Installing dependencies..."
          source /home/vboxuser/catty-app/venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt

          # 3. Перезапускаем приложение через systemd
          echo "Restarting application via systemd..."
          sudo systemctl restart $SERVICE_NAME

          # 4. Проверяем статус сервиса
          echo "Checking service status..."
          sleep 5
          if sudo systemctl is-active --quiet $SERVICE_NAME; then
            echo "✅ Service $SERVICE_NAME is running"
          else
            echo "❌ Service $SERVICE_NAME failed to start"
            sudo systemctl status $SERVICE_NAME -l
            exit 1
          fi

          # 5. Проверяем что приложение доступно
          echo "Checking application health..."
          if curl -f http://localhost:8181/ > /dev/null 2>&1; then
            echo "✅ Application is responding on port 8181"
            echo "🌐 Application available at: http://app.ushakov.course.prafdin.ru"
          else
            echo "❌ Application is not responding on port 8181"
            exit 1
          fi

          echo "🚀 Deployment completed successfully!"
